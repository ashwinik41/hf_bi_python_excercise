#!/bin/bash

#Note:
#Ideally we should create a DockerFile , install all required dependencies and run docker-image in a large project.
#For the sake of simplicity of this assignment, we have install_packages.sh to install dependencies,etc


# Install packages listed in requirements.txt
pip install -r requirements.txt
